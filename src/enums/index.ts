export enum Gender {
  Male = 1,
  Female = 2,
}

export enum Status {
  Disable = 0,
  Enable = 1,
}

export enum ManagerType {
  System = 1,
  General = 2,
}

export enum TopicType {
  Radio = 1,
  Checkbox = 2,
  Input = 3,
}
